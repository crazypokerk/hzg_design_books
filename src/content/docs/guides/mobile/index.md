---
title: 移动端手册总览
description: 面向手机和 PDA 场景的移动端最佳实践目录与阅读路径。
sidebar:
  order: 1
---

这套手册用于补充“设计目标”之外的落地规范，覆盖手机浏览器、企业微信/钉钉/飞书 `WebView`、`App` 内嵌 `H5`，以及 `PDA` 浏览器和 `PDA WebView`。

## 结构说明

手册主体按 5 个维度组织，而不是按单个组件拆分。这样更适合做方法论、评审标准和设计规范沉淀。

## 阅读顺序

1. [页面设计目标](../design_target/)
2. [目标与场景](./goals-scenarios/)
3. [信息架构](./information-architecture/)
4. [交互与行为](./interaction-behavior/)
5. [视觉与感知](./visual-perception/)
6. [性能](./performance/)

## 适用范围

- 手机业务页面：审批、查询、录入、详情、异常处理
- 企业应用 `H5`：企业微信、钉钉、飞书、`App WebView`
- `PDA` 作业页面：扫码、盘点、上架、出库、巡检、签收
- 混合设备场景：同一业务同时跑在手机和 `PDA` 上

## 使用原则

- 先定义任务，再定义界面。页面结构要围绕任务完成率，不围绕字段堆砌。
- 先保证最低可用，再追求高保真。尤其要先验证小屏、弱网、旧 `WebView` 和连续操作场景。
- 先控制复杂度，再补充能力。移动端容错空间比桌面端更小。
- 同一业务在手机与 `PDA` 上可以复用规则，但不应强行复用完全相同的交互。

## 建议产出

在设计或评审一个移动端页面时，至少同步产出下面内容：

- 页面目标与成功标准
- 页面信息架构草图
- 主流程与异常流程
- 关键交互与状态定义
- 视觉层级与重点信息标注
- 设备适配边界
- 性能与验收清单

## 推荐基线

| 项目 | 推荐值 |
| --- | --- |
| 主设计宽度 | 360px 或 375px |
| 最小验证宽度 | 320px |
| `PDA` 验证宽度 | 240px、280px、320px |
| 常规按钮高度 | 不小于 44px |
| `PDA` 高频按钮高度 | 不小于 48px |
| 正文字号 | 不小于 14px |
| 图标点击热区 | 不小于 40px x 40px |
| 相邻点击区间距 | 不小于 8px |
